[build]
  base = "frontend"
  command = "npm run build"
  publish = "frontend/dist"

# SPA redirect — all routes fallback to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
